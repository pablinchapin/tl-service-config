server :
  port : 8990

spring :
  config :
    name : service-config

  profiles :
    include : native

  cloud:
    config :
      server :
        native :
          searchLocations : classpath:/config-repo

